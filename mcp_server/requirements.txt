# Dependencies for the MCP server (separate from the web app's requirements.txt).
# httpx ships with the web app already (via openai) but is listed here so the
# MCP server can be installed standalone.
mcp>=1.2.0
httpx>=0.27.0
